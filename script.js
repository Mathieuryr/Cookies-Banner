const btnSucess = document.querySelector(".btn-success");

const cookies = document.querySelector(".cookies");

cookies.addEventListener("click", function () {
  cookies.style.opacity = "0";
  cookies.style.transition = "0.8s";
});
